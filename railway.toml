[build]
builder = "nixpacks"
buildCommand = "cd backend && npm install"
watchPatterns = ["backend/**/*"]

[deploy]
startCommand = "cd backend && npm start"
healthcheckPath = "/health"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
builderCommand = "cd backend"